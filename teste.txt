char dataCliente[9];
    int dia, mes, ano;
    printf("Digite sua data de nascimento (DD/MM/AAAA): ");
    fgets(dataCliente, sizeof(dataCliente), stdin);

    // Verfica se o sscanf conseguiu pegar 3 inteiros
    if (sscanf(dataCliente, "%d/%d/%d", &dia, &mes, &ano) == 3) {
        valida = validarData(dia, mes, ano);
        if (valida == 1) {
            return dataCliente;
        } else {
            return 0;
        }
    }
    

    me ajude a ajustar o código corretamente, ao iniciar o cadastro do cliente, pensei em criar uma variavel responsavel pela posicao do cliente, em seguida, no cadastrar cliente, ele deve informar o cpf e verificar se o cpf é cadastrado ou não no sistema, me ajude a deixar funcionando. ""